# Ignorar la carpeta de dependencias, que se puede reinstalar
node_modules

# Datos persistentes (viven en el volumen de Docker, no en el repo)
data/

# Secretos
.env

# Ignorar archivos de sistema operativo
.DS_Store